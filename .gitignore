.DS_Store
*.log
node_modules/
dist/
.idea/
.vscode/
*.swp
*.tmp

# Claude Code session-management directories — not part of cairn's product.
# (cairn ships .claude/ TEMPLATE content via files/, but the local .claude/ at repo
# root belongs to whatever session is editing cairn, not cairn itself.)
.claude/

