# /prior-art-scan — Skill Idea — Note

> **Filed:** 2026-06-09 · **By:** Claude Opus 4.7 (via fresh-agent `/session-distill` on foundational cairn build session, NEW_LAWS lens) · **Status:** open (gate-watch, 1/3 instances)

The second fresh-agent `/session-distill` of cairn's foundational build session (`docs/origin/748aff00-...jsonl`) — lens-shifted via `cwar/docs/NEW_LAWS_OF_AI_AGENT_ENGINEERING.md` — surfaced an `[LAW amnesia-tax]` (NEW_LAWS Law 3) violation that the original research papers and the first manual distillation didn't name: at L1606 of the foundational session, the agent confessed in real time that the entire LAWS.md concept had been designed without reading `cwar/docs/NEW_LAWS_OF_AI_AGENT_ENGINEERING.md` sitting in the same project tree. Quote: *"I didn't read it. I built cairn's LAWS concept from scratch as if the domain had no prior art... That's exactly the failure mode NEW_LAWS's own Law 3 (the Amnesia Tax) describes."* The first ~1600 lines of architectural decisions in cairn — the LAWS shape, citation discipline, mechanical-enforcement instinct — were made blind to prior art that already existed in the adjacent project's `docs/`. The agent had read CLAUDE.md but not the deeper meta-disciplinary file in the same tree. **Candidate skill:** `/prior-art-scan` — before designing any system whose problem-shape overlaps an existing doc in this repo's `docs/`, grep for `LAWS|RULES|PRINCIPLES|ENGINEERING|NEW_LAWS|META|DISCIPLINE` and read at least the headings before drafting. Alternative shape: fold as `/plan` Step 0 (pre-plan) rather than its own skill. Source: foundational session L1594-1606. 3-instance gate per `NOTE_CAIRN_INTROSPECT_SKILL_2026-04-26.md` (now promoted to `/session-distill`): currently **1/3** — watch for recurrence in future distillations.

- **Why this isn't `[LAW load-meta-laws]` (existing cairn dev law):** `load-meta-laws` fires *at session start* — search for LAWS files in the project. That's a session-startup probe. The Amnesia Tax violation happened at *design time within a single session* — when starting to architect a meta-system mid-session, the agent didn't pause to scan adjacent docs for prior art on the same meta-level. Different timing, different trigger.
- **Why this might be `/plan` Step 0 rather than a new skill:** the trigger ("before designing X, scan for prior art on X") fires inside the planning workflow. May not need its own verb if `/plan` covers it. The just-shipped `/plan` Step 7 (scope-checkpoint trigger) is structurally similar — it's a re-plan trigger keyed on a specific event class.
- **Gate-watch signal:** if a future distillation surfaces an agent designing a meta-system blind to prior art that existed in adjacent docs of the same project, count as +1 toward gate.

---

*Note — pre-emptive intent capture. If two more instances surface, promote via `/spec --from docs/notes/NOTE_PRIOR_ART_SCAN_CANDIDATE_2026-06-09.md`.*
