# GitHub personal access token with `issues:write` on the target repo.
# In Cloud Run, provide this via Secret Manager rather than a literal env var.
GITHUB_TOKEN=

# Target repo (owner/name). Defaults to winnorton/cairn when unset.
GITHUB_REPO=winnorton/cairn

# Optional override for the HTTP port. Cloud Run sets this automatically.
PORT=8080
